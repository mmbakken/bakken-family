// Enum of all allowed step values.
export const STEPS = {
  ENTRY: 0,
  MAIN: 1,
  LODGING: 2,
  DECLINED: 3,
  DONE: 4,
}

// Enum of types of attending values.
export const ATTENDING_STATUS = {
  DECLINED: -1,
  PENDING: 0,
  ACCEPTED: 1,
}
