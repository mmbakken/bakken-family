import { useTitle } from '@/hooks'
import { Button } from '@/components/ui/button'

const Declined = () => {
  useTitle('Wedding - RSVP - Declined')

  return <div>TODO: Implement RSVP - Declined</div>
}

export default Declined
